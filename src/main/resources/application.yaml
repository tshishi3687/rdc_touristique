spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rdc_tourism?serverTimezone=UTC
    username: root
    password: lilo
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl:   true
    show-sql:   false
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
server:
  port: 8081

